const pm = new ProductManager('./');

const searchProductBy = (param) => {
    if (param == 'id') {
        
    } else if (param == 'name') {

    } else if (param == 'category') {

    } else if (param == 'tag') {

    }
}